global:
  scrape_interval: 3s
  evaluation_interval: 3s

storage:
  tsdb:
    out_of_order_time_window: 30m

scrape_configs:
  - job_name: "apisix"
    metrics_path: /apisix/prometheus/metrics
    static_configs:
      - targets: ["apisix:9091"]
